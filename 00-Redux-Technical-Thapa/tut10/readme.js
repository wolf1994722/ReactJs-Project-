/* 
    TODO: Maximizing Code Reuse with Redux Toolkit's createAction Function
    ? it we delete base of extraReducer then it is off no use its alternative is createAction
    ? check deleteAllUseer.jsx
    ? we have to copy the extraReducer path in every code
*/ 