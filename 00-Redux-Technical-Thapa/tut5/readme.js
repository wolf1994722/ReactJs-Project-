/* 
    TODO: Access the state data with useDispatch & action creators
    ? npm i chance , for fake data
    ? 2 steps
        * useDispatch - b/c it will trigger actions methods
        * action Creators
*/ 