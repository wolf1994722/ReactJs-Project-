/* 
    TODO: Folder Structure, Prerequisites, and Homepage Design
    ? npm create vite
    ? cd /
    ? npm i
    ? npm i styled-components @reduxjs/toolkit react-redux react-icons --legacy-peer-deps
    ? npm run dev
    ? space option 2
*/