/* 
    TODO: Delete All Users using ExtraReducers in Redux Toolkit
    ? when multiple scile have common functionality then we can put it in ExtraReducers
    ? it is like function, in this extraReducers we define functionlaity which is common in more than one slice
*/ 