/* 
    TODO: JSX Attributes
    ? <a href=""> , html tag attribute like href
    ? HTML Global Attributes i.e title, class, id
    ? JSX camelcase in attribute contenteditable -> contentEditable
    ? if any attribute do~ have children like image then use </> (self closing tag)
*/