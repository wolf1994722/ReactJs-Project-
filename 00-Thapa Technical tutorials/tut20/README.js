/* 
    TODO: Hooks
    ? onClick={Increment}
    ? Destructring
    ? we can~ change state of variable in tradition event Handling in js
    ? [state_object, update_function] = useState()
*/

const name = ['vishal', 'vivek', 'gymmy']
const [name1, name2, name3] = name

console.log(name1,name2, name3)