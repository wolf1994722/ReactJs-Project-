/* 
    TODO: Handling Complex Multiple Input Form 
    ? we are using object instead of indivsual hook for each input
        * each input must have name attribute
    ? setState(()=>{}) can be used to know previous state of object or variable
        * const [var, setVar] = useSate()
        * setVar is know as setSate which carry previous state
*/ 