/* 
    TODO: Modules Import Export
    ? all we have done with data.js
    ? import defualt_export, { n_default_export_with exact name } from ''
    ! import * as data from './static/data' 
        * variable use are like ( data.default, data.n_default_exact_name)
*/