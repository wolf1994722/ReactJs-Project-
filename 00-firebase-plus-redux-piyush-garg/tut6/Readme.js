/*
    TODO: Firebase Google Authentication React
    ! clean code of google auth into context
    ? if looking for user login then check screenshot 22
      * if (user === null) {
      * return (
      *   <>
      *   <h1>welcome to Home 🔥🐇</h1>
      *   </>
      * );
      * } else {
      *   return (
      *     <>
      *     <h1>welcome to Home 🔥🐇, {user.name}</h1>
      *     <button>Logout</button>
      *     </>
      *   );
      * }
*/