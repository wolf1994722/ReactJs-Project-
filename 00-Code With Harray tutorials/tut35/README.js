/* 
    TODO: Understanding Password Hashing, Salt & Pepper 
    ? Hashing ,one way function, can~ be reversed
    ? Rainbow table are there of common pwd hash table of hacker
        * handle using salt
        * use bcrypt function genSalt
        * password = hash(pwd) + salt + pepper
*/