/* 
    TODO: Adding "fetch notes" functionality to iNotebook 
    ? fetch with headers
    ? npm i cors
    ? updateNote in Notes.js
    ! edit is imp, in components/Notes.js
    ? useRef , we can give a refrence to a given variable
    ? Add onClick on submit button of form
        * <button
        *     type="button"
        *     onClick={sumbitNote}>
        *     Save changes
        * </button>
*/