/* 
    TODO: React Component Lifecycle & Lifecycle methods
    ? if we want to use props in constructor then we have to pass props
        * constructor(props) {
            * super(props)
            * document.title = props.category 
        * }
    ? 
*/