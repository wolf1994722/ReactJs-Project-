/* 
    TODO: Adding Frontend Validation + Few Fixes in iNotebook
    ? if event on button
        * <button disabled={note.title.length < 5 || note.description.length < 5} >
        *     Save changes
        * </button>
    ? if event on form
        * <input minLength={5} required />
*/