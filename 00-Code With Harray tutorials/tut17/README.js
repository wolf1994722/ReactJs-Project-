/* 
    TODO: Understanding state in class based components 
    ? constructor 
        * constructor() {   
            * super()
            * this.state = {}
        * }
        * access var using this.state._____
    ? bootstrap -> btn-sm
    ? it will work as unique identify using newsUrl
    ? ctrl + shift + R -> for remove all cache and reload
*/