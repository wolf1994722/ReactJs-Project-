/* 
    TODO: JSX - js + xml
    ? <a  href="#"></a> -> href="/"
    ? using js with html
        * class -> className
        * for -> htmlFor
        * tab -> tabIndex
    ? return () , is cuz js automatically make semicolon after each div, so to avoid that
    ? const external_name = '<b>new watch</b>'
        * {external_name}
    ? babel is used to complie jsx
 */

