/* 
    TODO: Fetching News category wise in NewsMonkey
    ? exact will be used when component it ~ render for multiple anchor
    ? my component is ~ remount when anchor is changed
        * <News key={item} /> is help to render with new props
    ? imp b/c of change in syntax in routers
*/