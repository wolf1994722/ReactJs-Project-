/* 
    TODO: Props and PropTypes
    ? {b,c,d} name must be same while import & export
    ? impt -> import PropTypes from 'prop-types'
    ? Navbar.PropTypes = (pts) PropTypes.string
        * Navbar.propTypes = {
            * title: PropTypes.string.isRequired,
            * lists: PropTypes.arrayOf(PropTypes.string).isRequired,
        * }
        * defaultProps
    ? if one value is missing in required then it will we taken from .defaultProps
*/
