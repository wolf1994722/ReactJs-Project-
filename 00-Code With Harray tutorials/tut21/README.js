/* 
    TODO: Adding Categories & propTypes to NewsMonkey React App
    ? impt for importing proptypes or rcep
    ? pstr -> PropTypes.string.isRequired, 
    ? pnr -> PropTypes.number.isRequired, 
*/