/* 
    TODO: Accessing State in Redux
    ? navbar useSelector
    TODO: Updating state from a different component in Redux
    ? onClick={() => dispatch(actionCreators.incMoney(price))}
        * const { incMoney, decMoney } = bindActionCreators(actionCreators, dispatch)
*/