/* 
    TODO: Adding Previous & Next Buttons to populate NewsItems
    ? we must call it 
        * this.incrementPage
        * do ~ use const keyword while defining function
        * function must be defined after constructor and before render
    ? disabled={this.state.page <= 1}
    ? disabled={this.state.page >= Math.ceil(this.state.totalArticle/18)}
    ? network slow 3g
*/