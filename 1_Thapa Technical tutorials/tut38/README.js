/* 
    TODO: useEffect Hook = componentDidMount + componentDidUpdate + componentDidUnMount
                        * mount + unmount + update
    ? useEffect is call every time when there is rendering
    ? it is used to tell after render what you have to do for DOM update
    ? Hook are only used inside functionally component
    ? useState is give more priority to callback insted of rendering first
    ? useEffect(() => { }, [target_useState]) it will only run first time b/c []
*/